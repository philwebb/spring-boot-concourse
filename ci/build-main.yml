---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: springboot/ci-base-8-jdk

inputs:
- name: spring-boot

run:
  path: ls
  args: [spring-boot/ci, -l]

#  path: spring-boot/ci/build-main.sh
